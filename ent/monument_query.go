// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/dkrasnovdev/heritage-api/ent/artifact"
	"github.com/dkrasnovdev/heritage-api/ent/monument"
	"github.com/dkrasnovdev/heritage-api/ent/predicate"
	"github.com/dkrasnovdev/heritage-api/ent/set"
)

// MonumentQuery is the builder for querying Monument entities.
type MonumentQuery struct {
	config
	ctx                *QueryContext
	order              []monument.OrderOption
	inters             []Interceptor
	predicates         []predicate.Monument
	withArtifacts      *ArtifactQuery
	withSets           *SetQuery
	modifiers          []func(*sql.Selector)
	loadTotal          []func(context.Context, []*Monument) error
	withNamedArtifacts map[string]*ArtifactQuery
	withNamedSets      map[string]*SetQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MonumentQuery builder.
func (mq *MonumentQuery) Where(ps ...predicate.Monument) *MonumentQuery {
	mq.predicates = append(mq.predicates, ps...)
	return mq
}

// Limit the number of records to be returned by this query.
func (mq *MonumentQuery) Limit(limit int) *MonumentQuery {
	mq.ctx.Limit = &limit
	return mq
}

// Offset to start from.
func (mq *MonumentQuery) Offset(offset int) *MonumentQuery {
	mq.ctx.Offset = &offset
	return mq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mq *MonumentQuery) Unique(unique bool) *MonumentQuery {
	mq.ctx.Unique = &unique
	return mq
}

// Order specifies how the records should be ordered.
func (mq *MonumentQuery) Order(o ...monument.OrderOption) *MonumentQuery {
	mq.order = append(mq.order, o...)
	return mq
}

// QueryArtifacts chains the current query on the "artifacts" edge.
func (mq *MonumentQuery) QueryArtifacts() *ArtifactQuery {
	query := (&ArtifactClient{config: mq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := mq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := mq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(monument.Table, monument.FieldID, selector),
			sqlgraph.To(artifact.Table, artifact.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, monument.ArtifactsTable, monument.ArtifactsColumn),
		)
		fromU = sqlgraph.SetNeighbors(mq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySets chains the current query on the "sets" edge.
func (mq *MonumentQuery) QuerySets() *SetQuery {
	query := (&SetClient{config: mq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := mq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := mq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(monument.Table, monument.FieldID, selector),
			sqlgraph.To(set.Table, set.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, monument.SetsTable, monument.SetsPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(mq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Monument entity from the query.
// Returns a *NotFoundError when no Monument was found.
func (mq *MonumentQuery) First(ctx context.Context) (*Monument, error) {
	nodes, err := mq.Limit(1).All(setContextOp(ctx, mq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{monument.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mq *MonumentQuery) FirstX(ctx context.Context) *Monument {
	node, err := mq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Monument ID from the query.
// Returns a *NotFoundError when no Monument ID was found.
func (mq *MonumentQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = mq.Limit(1).IDs(setContextOp(ctx, mq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{monument.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mq *MonumentQuery) FirstIDX(ctx context.Context) int {
	id, err := mq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Monument entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Monument entity is found.
// Returns a *NotFoundError when no Monument entities are found.
func (mq *MonumentQuery) Only(ctx context.Context) (*Monument, error) {
	nodes, err := mq.Limit(2).All(setContextOp(ctx, mq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{monument.Label}
	default:
		return nil, &NotSingularError{monument.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mq *MonumentQuery) OnlyX(ctx context.Context) *Monument {
	node, err := mq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Monument ID in the query.
// Returns a *NotSingularError when more than one Monument ID is found.
// Returns a *NotFoundError when no entities are found.
func (mq *MonumentQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = mq.Limit(2).IDs(setContextOp(ctx, mq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{monument.Label}
	default:
		err = &NotSingularError{monument.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mq *MonumentQuery) OnlyIDX(ctx context.Context) int {
	id, err := mq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Monuments.
func (mq *MonumentQuery) All(ctx context.Context) ([]*Monument, error) {
	ctx = setContextOp(ctx, mq.ctx, "All")
	if err := mq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Monument, *MonumentQuery]()
	return withInterceptors[[]*Monument](ctx, mq, qr, mq.inters)
}

// AllX is like All, but panics if an error occurs.
func (mq *MonumentQuery) AllX(ctx context.Context) []*Monument {
	nodes, err := mq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Monument IDs.
func (mq *MonumentQuery) IDs(ctx context.Context) (ids []int, err error) {
	if mq.ctx.Unique == nil && mq.path != nil {
		mq.Unique(true)
	}
	ctx = setContextOp(ctx, mq.ctx, "IDs")
	if err = mq.Select(monument.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mq *MonumentQuery) IDsX(ctx context.Context) []int {
	ids, err := mq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mq *MonumentQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, mq.ctx, "Count")
	if err := mq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, mq, querierCount[*MonumentQuery](), mq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (mq *MonumentQuery) CountX(ctx context.Context) int {
	count, err := mq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mq *MonumentQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, mq.ctx, "Exist")
	switch _, err := mq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (mq *MonumentQuery) ExistX(ctx context.Context) bool {
	exist, err := mq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MonumentQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mq *MonumentQuery) Clone() *MonumentQuery {
	if mq == nil {
		return nil
	}
	return &MonumentQuery{
		config:        mq.config,
		ctx:           mq.ctx.Clone(),
		order:         append([]monument.OrderOption{}, mq.order...),
		inters:        append([]Interceptor{}, mq.inters...),
		predicates:    append([]predicate.Monument{}, mq.predicates...),
		withArtifacts: mq.withArtifacts.Clone(),
		withSets:      mq.withSets.Clone(),
		// clone intermediate query.
		sql:  mq.sql.Clone(),
		path: mq.path,
	}
}

// WithArtifacts tells the query-builder to eager-load the nodes that are connected to
// the "artifacts" edge. The optional arguments are used to configure the query builder of the edge.
func (mq *MonumentQuery) WithArtifacts(opts ...func(*ArtifactQuery)) *MonumentQuery {
	query := (&ArtifactClient{config: mq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	mq.withArtifacts = query
	return mq
}

// WithSets tells the query-builder to eager-load the nodes that are connected to
// the "sets" edge. The optional arguments are used to configure the query builder of the edge.
func (mq *MonumentQuery) WithSets(opts ...func(*SetQuery)) *MonumentQuery {
	query := (&SetClient{config: mq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	mq.withSets = query
	return mq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Monument.Query().
//		GroupBy(monument.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (mq *MonumentQuery) GroupBy(field string, fields ...string) *MonumentGroupBy {
	mq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MonumentGroupBy{build: mq}
	grbuild.flds = &mq.ctx.Fields
	grbuild.label = monument.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.Monument.Query().
//		Select(monument.FieldCreatedAt).
//		Scan(ctx, &v)
func (mq *MonumentQuery) Select(fields ...string) *MonumentSelect {
	mq.ctx.Fields = append(mq.ctx.Fields, fields...)
	sbuild := &MonumentSelect{MonumentQuery: mq}
	sbuild.label = monument.Label
	sbuild.flds, sbuild.scan = &mq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MonumentSelect configured with the given aggregations.
func (mq *MonumentQuery) Aggregate(fns ...AggregateFunc) *MonumentSelect {
	return mq.Select().Aggregate(fns...)
}

func (mq *MonumentQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range mq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, mq); err != nil {
				return err
			}
		}
	}
	for _, f := range mq.ctx.Fields {
		if !monument.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if mq.path != nil {
		prev, err := mq.path(ctx)
		if err != nil {
			return err
		}
		mq.sql = prev
	}
	if monument.Policy == nil {
		return errors.New("ent: uninitialized monument.Policy (forgotten import ent/runtime?)")
	}
	if err := monument.Policy.EvalQuery(ctx, mq); err != nil {
		return err
	}
	return nil
}

func (mq *MonumentQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Monument, error) {
	var (
		nodes       = []*Monument{}
		_spec       = mq.querySpec()
		loadedTypes = [2]bool{
			mq.withArtifacts != nil,
			mq.withSets != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Monument).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Monument{config: mq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(mq.modifiers) > 0 {
		_spec.Modifiers = mq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := mq.withArtifacts; query != nil {
		if err := mq.loadArtifacts(ctx, query, nodes,
			func(n *Monument) { n.Edges.Artifacts = []*Artifact{} },
			func(n *Monument, e *Artifact) { n.Edges.Artifacts = append(n.Edges.Artifacts, e) }); err != nil {
			return nil, err
		}
	}
	if query := mq.withSets; query != nil {
		if err := mq.loadSets(ctx, query, nodes,
			func(n *Monument) { n.Edges.Sets = []*Set{} },
			func(n *Monument, e *Set) { n.Edges.Sets = append(n.Edges.Sets, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range mq.withNamedArtifacts {
		if err := mq.loadArtifacts(ctx, query, nodes,
			func(n *Monument) { n.appendNamedArtifacts(name) },
			func(n *Monument, e *Artifact) { n.appendNamedArtifacts(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range mq.withNamedSets {
		if err := mq.loadSets(ctx, query, nodes,
			func(n *Monument) { n.appendNamedSets(name) },
			func(n *Monument, e *Set) { n.appendNamedSets(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range mq.loadTotal {
		if err := mq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (mq *MonumentQuery) loadArtifacts(ctx context.Context, query *ArtifactQuery, nodes []*Monument, init func(*Monument), assign func(*Monument, *Artifact)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Monument)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Artifact(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(monument.ArtifactsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.monument_artifacts
		if fk == nil {
			return fmt.Errorf(`foreign-key "monument_artifacts" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "monument_artifacts" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (mq *MonumentQuery) loadSets(ctx context.Context, query *SetQuery, nodes []*Monument, init func(*Monument), assign func(*Monument, *Set)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[int]*Monument)
	nids := make(map[int]map[*Monument]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(monument.SetsTable)
		s.Join(joinT).On(s.C(set.FieldID), joinT.C(monument.SetsPrimaryKey[1]))
		s.Where(sql.InValues(joinT.C(monument.SetsPrimaryKey[0]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(monument.SetsPrimaryKey[0]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(sql.NullInt64)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := int(values[0].(*sql.NullInt64).Int64)
				inValue := int(values[1].(*sql.NullInt64).Int64)
				if nids[inValue] == nil {
					nids[inValue] = map[*Monument]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*Set](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "sets" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}

func (mq *MonumentQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := mq.querySpec()
	if len(mq.modifiers) > 0 {
		_spec.Modifiers = mq.modifiers
	}
	_spec.Node.Columns = mq.ctx.Fields
	if len(mq.ctx.Fields) > 0 {
		_spec.Unique = mq.ctx.Unique != nil && *mq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, mq.driver, _spec)
}

func (mq *MonumentQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(monument.Table, monument.Columns, sqlgraph.NewFieldSpec(monument.FieldID, field.TypeInt))
	_spec.From = mq.sql
	if unique := mq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if mq.path != nil {
		_spec.Unique = true
	}
	if fields := mq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, monument.FieldID)
		for i := range fields {
			if fields[i] != monument.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := mq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mq *MonumentQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mq.driver.Dialect())
	t1 := builder.Table(monument.Table)
	columns := mq.ctx.Fields
	if len(columns) == 0 {
		columns = monument.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mq.sql != nil {
		selector = mq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mq.ctx.Unique != nil && *mq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range mq.predicates {
		p(selector)
	}
	for _, p := range mq.order {
		p(selector)
	}
	if offset := mq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedArtifacts tells the query-builder to eager-load the nodes that are connected to the "artifacts"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (mq *MonumentQuery) WithNamedArtifacts(name string, opts ...func(*ArtifactQuery)) *MonumentQuery {
	query := (&ArtifactClient{config: mq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if mq.withNamedArtifacts == nil {
		mq.withNamedArtifacts = make(map[string]*ArtifactQuery)
	}
	mq.withNamedArtifacts[name] = query
	return mq
}

// WithNamedSets tells the query-builder to eager-load the nodes that are connected to the "sets"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (mq *MonumentQuery) WithNamedSets(name string, opts ...func(*SetQuery)) *MonumentQuery {
	query := (&SetClient{config: mq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if mq.withNamedSets == nil {
		mq.withNamedSets = make(map[string]*SetQuery)
	}
	mq.withNamedSets[name] = query
	return mq
}

// MonumentGroupBy is the group-by builder for Monument entities.
type MonumentGroupBy struct {
	selector
	build *MonumentQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mgb *MonumentGroupBy) Aggregate(fns ...AggregateFunc) *MonumentGroupBy {
	mgb.fns = append(mgb.fns, fns...)
	return mgb
}

// Scan applies the selector query and scans the result into the given value.
func (mgb *MonumentGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mgb.build.ctx, "GroupBy")
	if err := mgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MonumentQuery, *MonumentGroupBy](ctx, mgb.build, mgb, mgb.build.inters, v)
}

func (mgb *MonumentGroupBy) sqlScan(ctx context.Context, root *MonumentQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(mgb.fns))
	for _, fn := range mgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*mgb.flds)+len(mgb.fns))
		for _, f := range *mgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*mgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MonumentSelect is the builder for selecting fields of Monument entities.
type MonumentSelect struct {
	*MonumentQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ms *MonumentSelect) Aggregate(fns ...AggregateFunc) *MonumentSelect {
	ms.fns = append(ms.fns, fns...)
	return ms
}

// Scan applies the selector query and scans the result into the given value.
func (ms *MonumentSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ms.ctx, "Select")
	if err := ms.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MonumentQuery, *MonumentSelect](ctx, ms.MonumentQuery, ms, ms.inters, v)
}

func (ms *MonumentSelect) sqlScan(ctx context.Context, root *MonumentQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ms.fns))
	for _, fn := range ms.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ms.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ms.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
