// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/dkrasnovdev/siberiana-api/ent/mound"
	"github.com/dkrasnovdev/siberiana-api/ent/petroglyph"
	"github.com/dkrasnovdev/siberiana-api/ent/predicate"
	"github.com/dkrasnovdev/siberiana-api/ent/visit"
)

// MoundQuery is the builder for querying Mound entities.
type MoundQuery struct {
	config
	ctx                  *QueryContext
	order                []mound.OrderOption
	inters               []Interceptor
	predicates           []predicate.Mound
	withPetroglyphs      *PetroglyphQuery
	withVisits           *VisitQuery
	modifiers            []func(*sql.Selector)
	loadTotal            []func(context.Context, []*Mound) error
	withNamedPetroglyphs map[string]*PetroglyphQuery
	withNamedVisits      map[string]*VisitQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MoundQuery builder.
func (mq *MoundQuery) Where(ps ...predicate.Mound) *MoundQuery {
	mq.predicates = append(mq.predicates, ps...)
	return mq
}

// Limit the number of records to be returned by this query.
func (mq *MoundQuery) Limit(limit int) *MoundQuery {
	mq.ctx.Limit = &limit
	return mq
}

// Offset to start from.
func (mq *MoundQuery) Offset(offset int) *MoundQuery {
	mq.ctx.Offset = &offset
	return mq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mq *MoundQuery) Unique(unique bool) *MoundQuery {
	mq.ctx.Unique = &unique
	return mq
}

// Order specifies how the records should be ordered.
func (mq *MoundQuery) Order(o ...mound.OrderOption) *MoundQuery {
	mq.order = append(mq.order, o...)
	return mq
}

// QueryPetroglyphs chains the current query on the "petroglyphs" edge.
func (mq *MoundQuery) QueryPetroglyphs() *PetroglyphQuery {
	query := (&PetroglyphClient{config: mq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := mq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := mq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(mound.Table, mound.FieldID, selector),
			sqlgraph.To(petroglyph.Table, petroglyph.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, mound.PetroglyphsTable, mound.PetroglyphsColumn),
		)
		fromU = sqlgraph.SetNeighbors(mq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryVisits chains the current query on the "visits" edge.
func (mq *MoundQuery) QueryVisits() *VisitQuery {
	query := (&VisitClient{config: mq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := mq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := mq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(mound.Table, mound.FieldID, selector),
			sqlgraph.To(visit.Table, visit.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, true, mound.VisitsTable, mound.VisitsPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(mq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Mound entity from the query.
// Returns a *NotFoundError when no Mound was found.
func (mq *MoundQuery) First(ctx context.Context) (*Mound, error) {
	nodes, err := mq.Limit(1).All(setContextOp(ctx, mq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{mound.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mq *MoundQuery) FirstX(ctx context.Context) *Mound {
	node, err := mq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Mound ID from the query.
// Returns a *NotFoundError when no Mound ID was found.
func (mq *MoundQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = mq.Limit(1).IDs(setContextOp(ctx, mq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{mound.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mq *MoundQuery) FirstIDX(ctx context.Context) int {
	id, err := mq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Mound entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Mound entity is found.
// Returns a *NotFoundError when no Mound entities are found.
func (mq *MoundQuery) Only(ctx context.Context) (*Mound, error) {
	nodes, err := mq.Limit(2).All(setContextOp(ctx, mq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{mound.Label}
	default:
		return nil, &NotSingularError{mound.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mq *MoundQuery) OnlyX(ctx context.Context) *Mound {
	node, err := mq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Mound ID in the query.
// Returns a *NotSingularError when more than one Mound ID is found.
// Returns a *NotFoundError when no entities are found.
func (mq *MoundQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = mq.Limit(2).IDs(setContextOp(ctx, mq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{mound.Label}
	default:
		err = &NotSingularError{mound.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mq *MoundQuery) OnlyIDX(ctx context.Context) int {
	id, err := mq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Mounds.
func (mq *MoundQuery) All(ctx context.Context) ([]*Mound, error) {
	ctx = setContextOp(ctx, mq.ctx, "All")
	if err := mq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Mound, *MoundQuery]()
	return withInterceptors[[]*Mound](ctx, mq, qr, mq.inters)
}

// AllX is like All, but panics if an error occurs.
func (mq *MoundQuery) AllX(ctx context.Context) []*Mound {
	nodes, err := mq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Mound IDs.
func (mq *MoundQuery) IDs(ctx context.Context) (ids []int, err error) {
	if mq.ctx.Unique == nil && mq.path != nil {
		mq.Unique(true)
	}
	ctx = setContextOp(ctx, mq.ctx, "IDs")
	if err = mq.Select(mound.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mq *MoundQuery) IDsX(ctx context.Context) []int {
	ids, err := mq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mq *MoundQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, mq.ctx, "Count")
	if err := mq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, mq, querierCount[*MoundQuery](), mq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (mq *MoundQuery) CountX(ctx context.Context) int {
	count, err := mq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mq *MoundQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, mq.ctx, "Exist")
	switch _, err := mq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (mq *MoundQuery) ExistX(ctx context.Context) bool {
	exist, err := mq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MoundQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mq *MoundQuery) Clone() *MoundQuery {
	if mq == nil {
		return nil
	}
	return &MoundQuery{
		config:          mq.config,
		ctx:             mq.ctx.Clone(),
		order:           append([]mound.OrderOption{}, mq.order...),
		inters:          append([]Interceptor{}, mq.inters...),
		predicates:      append([]predicate.Mound{}, mq.predicates...),
		withPetroglyphs: mq.withPetroglyphs.Clone(),
		withVisits:      mq.withVisits.Clone(),
		// clone intermediate query.
		sql:  mq.sql.Clone(),
		path: mq.path,
	}
}

// WithPetroglyphs tells the query-builder to eager-load the nodes that are connected to
// the "petroglyphs" edge. The optional arguments are used to configure the query builder of the edge.
func (mq *MoundQuery) WithPetroglyphs(opts ...func(*PetroglyphQuery)) *MoundQuery {
	query := (&PetroglyphClient{config: mq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	mq.withPetroglyphs = query
	return mq
}

// WithVisits tells the query-builder to eager-load the nodes that are connected to
// the "visits" edge. The optional arguments are used to configure the query builder of the edge.
func (mq *MoundQuery) WithVisits(opts ...func(*VisitQuery)) *MoundQuery {
	query := (&VisitClient{config: mq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	mq.withVisits = query
	return mq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Mound.Query().
//		GroupBy(mound.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (mq *MoundQuery) GroupBy(field string, fields ...string) *MoundGroupBy {
	mq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MoundGroupBy{build: mq}
	grbuild.flds = &mq.ctx.Fields
	grbuild.label = mound.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.Mound.Query().
//		Select(mound.FieldCreatedAt).
//		Scan(ctx, &v)
func (mq *MoundQuery) Select(fields ...string) *MoundSelect {
	mq.ctx.Fields = append(mq.ctx.Fields, fields...)
	sbuild := &MoundSelect{MoundQuery: mq}
	sbuild.label = mound.Label
	sbuild.flds, sbuild.scan = &mq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MoundSelect configured with the given aggregations.
func (mq *MoundQuery) Aggregate(fns ...AggregateFunc) *MoundSelect {
	return mq.Select().Aggregate(fns...)
}

func (mq *MoundQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range mq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, mq); err != nil {
				return err
			}
		}
	}
	for _, f := range mq.ctx.Fields {
		if !mound.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if mq.path != nil {
		prev, err := mq.path(ctx)
		if err != nil {
			return err
		}
		mq.sql = prev
	}
	if mound.Policy == nil {
		return errors.New("ent: uninitialized mound.Policy (forgotten import ent/runtime?)")
	}
	if err := mound.Policy.EvalQuery(ctx, mq); err != nil {
		return err
	}
	return nil
}

func (mq *MoundQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Mound, error) {
	var (
		nodes       = []*Mound{}
		_spec       = mq.querySpec()
		loadedTypes = [2]bool{
			mq.withPetroglyphs != nil,
			mq.withVisits != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Mound).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Mound{config: mq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(mq.modifiers) > 0 {
		_spec.Modifiers = mq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := mq.withPetroglyphs; query != nil {
		if err := mq.loadPetroglyphs(ctx, query, nodes,
			func(n *Mound) { n.Edges.Petroglyphs = []*Petroglyph{} },
			func(n *Mound, e *Petroglyph) { n.Edges.Petroglyphs = append(n.Edges.Petroglyphs, e) }); err != nil {
			return nil, err
		}
	}
	if query := mq.withVisits; query != nil {
		if err := mq.loadVisits(ctx, query, nodes,
			func(n *Mound) { n.Edges.Visits = []*Visit{} },
			func(n *Mound, e *Visit) { n.Edges.Visits = append(n.Edges.Visits, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range mq.withNamedPetroglyphs {
		if err := mq.loadPetroglyphs(ctx, query, nodes,
			func(n *Mound) { n.appendNamedPetroglyphs(name) },
			func(n *Mound, e *Petroglyph) { n.appendNamedPetroglyphs(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range mq.withNamedVisits {
		if err := mq.loadVisits(ctx, query, nodes,
			func(n *Mound) { n.appendNamedVisits(name) },
			func(n *Mound, e *Visit) { n.appendNamedVisits(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range mq.loadTotal {
		if err := mq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (mq *MoundQuery) loadPetroglyphs(ctx context.Context, query *PetroglyphQuery, nodes []*Mound, init func(*Mound), assign func(*Mound, *Petroglyph)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Mound)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Petroglyph(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(mound.PetroglyphsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.mound_petroglyphs
		if fk == nil {
			return fmt.Errorf(`foreign-key "mound_petroglyphs" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "mound_petroglyphs" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (mq *MoundQuery) loadVisits(ctx context.Context, query *VisitQuery, nodes []*Mound, init func(*Mound), assign func(*Mound, *Visit)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[int]*Mound)
	nids := make(map[int]map[*Mound]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(mound.VisitsTable)
		s.Join(joinT).On(s.C(visit.FieldID), joinT.C(mound.VisitsPrimaryKey[0]))
		s.Where(sql.InValues(joinT.C(mound.VisitsPrimaryKey[1]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(mound.VisitsPrimaryKey[1]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(sql.NullInt64)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := int(values[0].(*sql.NullInt64).Int64)
				inValue := int(values[1].(*sql.NullInt64).Int64)
				if nids[inValue] == nil {
					nids[inValue] = map[*Mound]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*Visit](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "visits" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}

func (mq *MoundQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := mq.querySpec()
	if len(mq.modifiers) > 0 {
		_spec.Modifiers = mq.modifiers
	}
	_spec.Node.Columns = mq.ctx.Fields
	if len(mq.ctx.Fields) > 0 {
		_spec.Unique = mq.ctx.Unique != nil && *mq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, mq.driver, _spec)
}

func (mq *MoundQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(mound.Table, mound.Columns, sqlgraph.NewFieldSpec(mound.FieldID, field.TypeInt))
	_spec.From = mq.sql
	if unique := mq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if mq.path != nil {
		_spec.Unique = true
	}
	if fields := mq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, mound.FieldID)
		for i := range fields {
			if fields[i] != mound.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := mq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mq *MoundQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mq.driver.Dialect())
	t1 := builder.Table(mound.Table)
	columns := mq.ctx.Fields
	if len(columns) == 0 {
		columns = mound.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mq.sql != nil {
		selector = mq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mq.ctx.Unique != nil && *mq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range mq.predicates {
		p(selector)
	}
	for _, p := range mq.order {
		p(selector)
	}
	if offset := mq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedPetroglyphs tells the query-builder to eager-load the nodes that are connected to the "petroglyphs"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (mq *MoundQuery) WithNamedPetroglyphs(name string, opts ...func(*PetroglyphQuery)) *MoundQuery {
	query := (&PetroglyphClient{config: mq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if mq.withNamedPetroglyphs == nil {
		mq.withNamedPetroglyphs = make(map[string]*PetroglyphQuery)
	}
	mq.withNamedPetroglyphs[name] = query
	return mq
}

// WithNamedVisits tells the query-builder to eager-load the nodes that are connected to the "visits"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (mq *MoundQuery) WithNamedVisits(name string, opts ...func(*VisitQuery)) *MoundQuery {
	query := (&VisitClient{config: mq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if mq.withNamedVisits == nil {
		mq.withNamedVisits = make(map[string]*VisitQuery)
	}
	mq.withNamedVisits[name] = query
	return mq
}

// MoundGroupBy is the group-by builder for Mound entities.
type MoundGroupBy struct {
	selector
	build *MoundQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mgb *MoundGroupBy) Aggregate(fns ...AggregateFunc) *MoundGroupBy {
	mgb.fns = append(mgb.fns, fns...)
	return mgb
}

// Scan applies the selector query and scans the result into the given value.
func (mgb *MoundGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mgb.build.ctx, "GroupBy")
	if err := mgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MoundQuery, *MoundGroupBy](ctx, mgb.build, mgb, mgb.build.inters, v)
}

func (mgb *MoundGroupBy) sqlScan(ctx context.Context, root *MoundQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(mgb.fns))
	for _, fn := range mgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*mgb.flds)+len(mgb.fns))
		for _, f := range *mgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*mgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MoundSelect is the builder for selecting fields of Mound entities.
type MoundSelect struct {
	*MoundQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ms *MoundSelect) Aggregate(fns ...AggregateFunc) *MoundSelect {
	ms.fns = append(ms.fns, fns...)
	return ms
}

// Scan applies the selector query and scans the result into the given value.
func (ms *MoundSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ms.ctx, "Select")
	if err := ms.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MoundQuery, *MoundSelect](ctx, ms.MoundQuery, ms, ms.inters, v)
}

func (ms *MoundSelect) sqlScan(ctx context.Context, root *MoundQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ms.fns))
	for _, fn := range ms.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ms.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ms.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
