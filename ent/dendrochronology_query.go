// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/dkrasnovdev/siberiana-api/ent/dendrochronologicalanalysis"
	"github.com/dkrasnovdev/siberiana-api/ent/dendrochronology"
	"github.com/dkrasnovdev/siberiana-api/ent/predicate"
)

// DendrochronologyQuery is the builder for querying Dendrochronology entities.
type DendrochronologyQuery struct {
	config
	ctx               *QueryContext
	order             []dendrochronology.OrderOption
	inters            []Interceptor
	predicates        []predicate.Dendrochronology
	withAnalysis      *DendrochronologicalAnalysisQuery
	withFKs           bool
	modifiers         []func(*sql.Selector)
	loadTotal         []func(context.Context, []*Dendrochronology) error
	withNamedAnalysis map[string]*DendrochronologicalAnalysisQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DendrochronologyQuery builder.
func (dq *DendrochronologyQuery) Where(ps ...predicate.Dendrochronology) *DendrochronologyQuery {
	dq.predicates = append(dq.predicates, ps...)
	return dq
}

// Limit the number of records to be returned by this query.
func (dq *DendrochronologyQuery) Limit(limit int) *DendrochronologyQuery {
	dq.ctx.Limit = &limit
	return dq
}

// Offset to start from.
func (dq *DendrochronologyQuery) Offset(offset int) *DendrochronologyQuery {
	dq.ctx.Offset = &offset
	return dq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (dq *DendrochronologyQuery) Unique(unique bool) *DendrochronologyQuery {
	dq.ctx.Unique = &unique
	return dq
}

// Order specifies how the records should be ordered.
func (dq *DendrochronologyQuery) Order(o ...dendrochronology.OrderOption) *DendrochronologyQuery {
	dq.order = append(dq.order, o...)
	return dq
}

// QueryAnalysis chains the current query on the "analysis" edge.
func (dq *DendrochronologyQuery) QueryAnalysis() *DendrochronologicalAnalysisQuery {
	query := (&DendrochronologicalAnalysisClient{config: dq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := dq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := dq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(dendrochronology.Table, dendrochronology.FieldID, selector),
			sqlgraph.To(dendrochronologicalanalysis.Table, dendrochronologicalanalysis.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, dendrochronology.AnalysisTable, dendrochronology.AnalysisColumn),
		)
		fromU = sqlgraph.SetNeighbors(dq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Dendrochronology entity from the query.
// Returns a *NotFoundError when no Dendrochronology was found.
func (dq *DendrochronologyQuery) First(ctx context.Context) (*Dendrochronology, error) {
	nodes, err := dq.Limit(1).All(setContextOp(ctx, dq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{dendrochronology.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (dq *DendrochronologyQuery) FirstX(ctx context.Context) *Dendrochronology {
	node, err := dq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Dendrochronology ID from the query.
// Returns a *NotFoundError when no Dendrochronology ID was found.
func (dq *DendrochronologyQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = dq.Limit(1).IDs(setContextOp(ctx, dq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{dendrochronology.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (dq *DendrochronologyQuery) FirstIDX(ctx context.Context) int {
	id, err := dq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Dendrochronology entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Dendrochronology entity is found.
// Returns a *NotFoundError when no Dendrochronology entities are found.
func (dq *DendrochronologyQuery) Only(ctx context.Context) (*Dendrochronology, error) {
	nodes, err := dq.Limit(2).All(setContextOp(ctx, dq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{dendrochronology.Label}
	default:
		return nil, &NotSingularError{dendrochronology.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (dq *DendrochronologyQuery) OnlyX(ctx context.Context) *Dendrochronology {
	node, err := dq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Dendrochronology ID in the query.
// Returns a *NotSingularError when more than one Dendrochronology ID is found.
// Returns a *NotFoundError when no entities are found.
func (dq *DendrochronologyQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = dq.Limit(2).IDs(setContextOp(ctx, dq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{dendrochronology.Label}
	default:
		err = &NotSingularError{dendrochronology.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (dq *DendrochronologyQuery) OnlyIDX(ctx context.Context) int {
	id, err := dq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Dendrochronologies.
func (dq *DendrochronologyQuery) All(ctx context.Context) ([]*Dendrochronology, error) {
	ctx = setContextOp(ctx, dq.ctx, "All")
	if err := dq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Dendrochronology, *DendrochronologyQuery]()
	return withInterceptors[[]*Dendrochronology](ctx, dq, qr, dq.inters)
}

// AllX is like All, but panics if an error occurs.
func (dq *DendrochronologyQuery) AllX(ctx context.Context) []*Dendrochronology {
	nodes, err := dq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Dendrochronology IDs.
func (dq *DendrochronologyQuery) IDs(ctx context.Context) (ids []int, err error) {
	if dq.ctx.Unique == nil && dq.path != nil {
		dq.Unique(true)
	}
	ctx = setContextOp(ctx, dq.ctx, "IDs")
	if err = dq.Select(dendrochronology.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (dq *DendrochronologyQuery) IDsX(ctx context.Context) []int {
	ids, err := dq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (dq *DendrochronologyQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, dq.ctx, "Count")
	if err := dq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, dq, querierCount[*DendrochronologyQuery](), dq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (dq *DendrochronologyQuery) CountX(ctx context.Context) int {
	count, err := dq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (dq *DendrochronologyQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, dq.ctx, "Exist")
	switch _, err := dq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (dq *DendrochronologyQuery) ExistX(ctx context.Context) bool {
	exist, err := dq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DendrochronologyQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (dq *DendrochronologyQuery) Clone() *DendrochronologyQuery {
	if dq == nil {
		return nil
	}
	return &DendrochronologyQuery{
		config:       dq.config,
		ctx:          dq.ctx.Clone(),
		order:        append([]dendrochronology.OrderOption{}, dq.order...),
		inters:       append([]Interceptor{}, dq.inters...),
		predicates:   append([]predicate.Dendrochronology{}, dq.predicates...),
		withAnalysis: dq.withAnalysis.Clone(),
		// clone intermediate query.
		sql:  dq.sql.Clone(),
		path: dq.path,
	}
}

// WithAnalysis tells the query-builder to eager-load the nodes that are connected to
// the "analysis" edge. The optional arguments are used to configure the query builder of the edge.
func (dq *DendrochronologyQuery) WithAnalysis(opts ...func(*DendrochronologicalAnalysisQuery)) *DendrochronologyQuery {
	query := (&DendrochronologicalAnalysisClient{config: dq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	dq.withAnalysis = query
	return dq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Dendrochronology.Query().
//		GroupBy(dendrochronology.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (dq *DendrochronologyQuery) GroupBy(field string, fields ...string) *DendrochronologyGroupBy {
	dq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DendrochronologyGroupBy{build: dq}
	grbuild.flds = &dq.ctx.Fields
	grbuild.label = dendrochronology.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.Dendrochronology.Query().
//		Select(dendrochronology.FieldCreatedAt).
//		Scan(ctx, &v)
func (dq *DendrochronologyQuery) Select(fields ...string) *DendrochronologySelect {
	dq.ctx.Fields = append(dq.ctx.Fields, fields...)
	sbuild := &DendrochronologySelect{DendrochronologyQuery: dq}
	sbuild.label = dendrochronology.Label
	sbuild.flds, sbuild.scan = &dq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DendrochronologySelect configured with the given aggregations.
func (dq *DendrochronologyQuery) Aggregate(fns ...AggregateFunc) *DendrochronologySelect {
	return dq.Select().Aggregate(fns...)
}

func (dq *DendrochronologyQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range dq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, dq); err != nil {
				return err
			}
		}
	}
	for _, f := range dq.ctx.Fields {
		if !dendrochronology.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if dq.path != nil {
		prev, err := dq.path(ctx)
		if err != nil {
			return err
		}
		dq.sql = prev
	}
	if dendrochronology.Policy == nil {
		return errors.New("ent: uninitialized dendrochronology.Policy (forgotten import ent/runtime?)")
	}
	if err := dendrochronology.Policy.EvalQuery(ctx, dq); err != nil {
		return err
	}
	return nil
}

func (dq *DendrochronologyQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Dendrochronology, error) {
	var (
		nodes       = []*Dendrochronology{}
		withFKs     = dq.withFKs
		_spec       = dq.querySpec()
		loadedTypes = [1]bool{
			dq.withAnalysis != nil,
		}
	)
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, dendrochronology.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Dendrochronology).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Dendrochronology{config: dq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(dq.modifiers) > 0 {
		_spec.Modifiers = dq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, dq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := dq.withAnalysis; query != nil {
		if err := dq.loadAnalysis(ctx, query, nodes,
			func(n *Dendrochronology) { n.Edges.Analysis = []*DendrochronologicalAnalysis{} },
			func(n *Dendrochronology, e *DendrochronologicalAnalysis) {
				n.Edges.Analysis = append(n.Edges.Analysis, e)
			}); err != nil {
			return nil, err
		}
	}
	for name, query := range dq.withNamedAnalysis {
		if err := dq.loadAnalysis(ctx, query, nodes,
			func(n *Dendrochronology) { n.appendNamedAnalysis(name) },
			func(n *Dendrochronology, e *DendrochronologicalAnalysis) { n.appendNamedAnalysis(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range dq.loadTotal {
		if err := dq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (dq *DendrochronologyQuery) loadAnalysis(ctx context.Context, query *DendrochronologicalAnalysisQuery, nodes []*Dendrochronology, init func(*Dendrochronology), assign func(*Dendrochronology, *DendrochronologicalAnalysis)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Dendrochronology)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.DendrochronologicalAnalysis(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(dendrochronology.AnalysisColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.dendrochronological_analysis_dendrochronology
		if fk == nil {
			return fmt.Errorf(`foreign-key "dendrochronological_analysis_dendrochronology" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "dendrochronological_analysis_dendrochronology" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (dq *DendrochronologyQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := dq.querySpec()
	if len(dq.modifiers) > 0 {
		_spec.Modifiers = dq.modifiers
	}
	_spec.Node.Columns = dq.ctx.Fields
	if len(dq.ctx.Fields) > 0 {
		_spec.Unique = dq.ctx.Unique != nil && *dq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, dq.driver, _spec)
}

func (dq *DendrochronologyQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(dendrochronology.Table, dendrochronology.Columns, sqlgraph.NewFieldSpec(dendrochronology.FieldID, field.TypeInt))
	_spec.From = dq.sql
	if unique := dq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if dq.path != nil {
		_spec.Unique = true
	}
	if fields := dq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, dendrochronology.FieldID)
		for i := range fields {
			if fields[i] != dendrochronology.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := dq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := dq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := dq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := dq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (dq *DendrochronologyQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(dq.driver.Dialect())
	t1 := builder.Table(dendrochronology.Table)
	columns := dq.ctx.Fields
	if len(columns) == 0 {
		columns = dendrochronology.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if dq.sql != nil {
		selector = dq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if dq.ctx.Unique != nil && *dq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range dq.predicates {
		p(selector)
	}
	for _, p := range dq.order {
		p(selector)
	}
	if offset := dq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := dq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedAnalysis tells the query-builder to eager-load the nodes that are connected to the "analysis"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (dq *DendrochronologyQuery) WithNamedAnalysis(name string, opts ...func(*DendrochronologicalAnalysisQuery)) *DendrochronologyQuery {
	query := (&DendrochronologicalAnalysisClient{config: dq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if dq.withNamedAnalysis == nil {
		dq.withNamedAnalysis = make(map[string]*DendrochronologicalAnalysisQuery)
	}
	dq.withNamedAnalysis[name] = query
	return dq
}

// DendrochronologyGroupBy is the group-by builder for Dendrochronology entities.
type DendrochronologyGroupBy struct {
	selector
	build *DendrochronologyQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (dgb *DendrochronologyGroupBy) Aggregate(fns ...AggregateFunc) *DendrochronologyGroupBy {
	dgb.fns = append(dgb.fns, fns...)
	return dgb
}

// Scan applies the selector query and scans the result into the given value.
func (dgb *DendrochronologyGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dgb.build.ctx, "GroupBy")
	if err := dgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DendrochronologyQuery, *DendrochronologyGroupBy](ctx, dgb.build, dgb, dgb.build.inters, v)
}

func (dgb *DendrochronologyGroupBy) sqlScan(ctx context.Context, root *DendrochronologyQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(dgb.fns))
	for _, fn := range dgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*dgb.flds)+len(dgb.fns))
		for _, f := range *dgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*dgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DendrochronologySelect is the builder for selecting fields of Dendrochronology entities.
type DendrochronologySelect struct {
	*DendrochronologyQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ds *DendrochronologySelect) Aggregate(fns ...AggregateFunc) *DendrochronologySelect {
	ds.fns = append(ds.fns, fns...)
	return ds
}

// Scan applies the selector query and scans the result into the given value.
func (ds *DendrochronologySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ds.ctx, "Select")
	if err := ds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DendrochronologyQuery, *DendrochronologySelect](ctx, ds.DendrochronologyQuery, ds, ds.inters, v)
}

func (ds *DendrochronologySelect) sqlScan(ctx context.Context, root *DendrochronologyQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ds.fns))
	for _, fn := range ds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
