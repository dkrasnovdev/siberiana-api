// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/dkrasnovdev/siberiana-api/ent/artifact"
	"github.com/dkrasnovdev/siberiana-api/ent/ethnos"
	"github.com/dkrasnovdev/siberiana-api/ent/predicate"
)

// EthnosQuery is the builder for querying Ethnos entities.
type EthnosQuery struct {
	config
	ctx                *QueryContext
	order              []ethnos.OrderOption
	inters             []Interceptor
	predicates         []predicate.Ethnos
	withArtifacts      *ArtifactQuery
	modifiers          []func(*sql.Selector)
	loadTotal          []func(context.Context, []*Ethnos) error
	withNamedArtifacts map[string]*ArtifactQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EthnosQuery builder.
func (eq *EthnosQuery) Where(ps ...predicate.Ethnos) *EthnosQuery {
	eq.predicates = append(eq.predicates, ps...)
	return eq
}

// Limit the number of records to be returned by this query.
func (eq *EthnosQuery) Limit(limit int) *EthnosQuery {
	eq.ctx.Limit = &limit
	return eq
}

// Offset to start from.
func (eq *EthnosQuery) Offset(offset int) *EthnosQuery {
	eq.ctx.Offset = &offset
	return eq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (eq *EthnosQuery) Unique(unique bool) *EthnosQuery {
	eq.ctx.Unique = &unique
	return eq
}

// Order specifies how the records should be ordered.
func (eq *EthnosQuery) Order(o ...ethnos.OrderOption) *EthnosQuery {
	eq.order = append(eq.order, o...)
	return eq
}

// QueryArtifacts chains the current query on the "artifacts" edge.
func (eq *EthnosQuery) QueryArtifacts() *ArtifactQuery {
	query := (&ArtifactClient{config: eq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := eq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := eq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(ethnos.Table, ethnos.FieldID, selector),
			sqlgraph.To(artifact.Table, artifact.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, ethnos.ArtifactsTable, ethnos.ArtifactsColumn),
		)
		fromU = sqlgraph.SetNeighbors(eq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Ethnos entity from the query.
// Returns a *NotFoundError when no Ethnos was found.
func (eq *EthnosQuery) First(ctx context.Context) (*Ethnos, error) {
	nodes, err := eq.Limit(1).All(setContextOp(ctx, eq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{ethnos.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (eq *EthnosQuery) FirstX(ctx context.Context) *Ethnos {
	node, err := eq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Ethnos ID from the query.
// Returns a *NotFoundError when no Ethnos ID was found.
func (eq *EthnosQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = eq.Limit(1).IDs(setContextOp(ctx, eq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{ethnos.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (eq *EthnosQuery) FirstIDX(ctx context.Context) int {
	id, err := eq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Ethnos entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Ethnos entity is found.
// Returns a *NotFoundError when no Ethnos entities are found.
func (eq *EthnosQuery) Only(ctx context.Context) (*Ethnos, error) {
	nodes, err := eq.Limit(2).All(setContextOp(ctx, eq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{ethnos.Label}
	default:
		return nil, &NotSingularError{ethnos.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (eq *EthnosQuery) OnlyX(ctx context.Context) *Ethnos {
	node, err := eq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Ethnos ID in the query.
// Returns a *NotSingularError when more than one Ethnos ID is found.
// Returns a *NotFoundError when no entities are found.
func (eq *EthnosQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = eq.Limit(2).IDs(setContextOp(ctx, eq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{ethnos.Label}
	default:
		err = &NotSingularError{ethnos.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (eq *EthnosQuery) OnlyIDX(ctx context.Context) int {
	id, err := eq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EthnosSlice.
func (eq *EthnosQuery) All(ctx context.Context) ([]*Ethnos, error) {
	ctx = setContextOp(ctx, eq.ctx, "All")
	if err := eq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Ethnos, *EthnosQuery]()
	return withInterceptors[[]*Ethnos](ctx, eq, qr, eq.inters)
}

// AllX is like All, but panics if an error occurs.
func (eq *EthnosQuery) AllX(ctx context.Context) []*Ethnos {
	nodes, err := eq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Ethnos IDs.
func (eq *EthnosQuery) IDs(ctx context.Context) (ids []int, err error) {
	if eq.ctx.Unique == nil && eq.path != nil {
		eq.Unique(true)
	}
	ctx = setContextOp(ctx, eq.ctx, "IDs")
	if err = eq.Select(ethnos.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (eq *EthnosQuery) IDsX(ctx context.Context) []int {
	ids, err := eq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (eq *EthnosQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, eq.ctx, "Count")
	if err := eq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, eq, querierCount[*EthnosQuery](), eq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (eq *EthnosQuery) CountX(ctx context.Context) int {
	count, err := eq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (eq *EthnosQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, eq.ctx, "Exist")
	switch _, err := eq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (eq *EthnosQuery) ExistX(ctx context.Context) bool {
	exist, err := eq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EthnosQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (eq *EthnosQuery) Clone() *EthnosQuery {
	if eq == nil {
		return nil
	}
	return &EthnosQuery{
		config:        eq.config,
		ctx:           eq.ctx.Clone(),
		order:         append([]ethnos.OrderOption{}, eq.order...),
		inters:        append([]Interceptor{}, eq.inters...),
		predicates:    append([]predicate.Ethnos{}, eq.predicates...),
		withArtifacts: eq.withArtifacts.Clone(),
		// clone intermediate query.
		sql:  eq.sql.Clone(),
		path: eq.path,
	}
}

// WithArtifacts tells the query-builder to eager-load the nodes that are connected to
// the "artifacts" edge. The optional arguments are used to configure the query builder of the edge.
func (eq *EthnosQuery) WithArtifacts(opts ...func(*ArtifactQuery)) *EthnosQuery {
	query := (&ArtifactClient{config: eq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	eq.withArtifacts = query
	return eq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Ethnos.Query().
//		GroupBy(ethnos.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (eq *EthnosQuery) GroupBy(field string, fields ...string) *EthnosGroupBy {
	eq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EthnosGroupBy{build: eq}
	grbuild.flds = &eq.ctx.Fields
	grbuild.label = ethnos.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.Ethnos.Query().
//		Select(ethnos.FieldCreatedAt).
//		Scan(ctx, &v)
func (eq *EthnosQuery) Select(fields ...string) *EthnosSelect {
	eq.ctx.Fields = append(eq.ctx.Fields, fields...)
	sbuild := &EthnosSelect{EthnosQuery: eq}
	sbuild.label = ethnos.Label
	sbuild.flds, sbuild.scan = &eq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EthnosSelect configured with the given aggregations.
func (eq *EthnosQuery) Aggregate(fns ...AggregateFunc) *EthnosSelect {
	return eq.Select().Aggregate(fns...)
}

func (eq *EthnosQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range eq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, eq); err != nil {
				return err
			}
		}
	}
	for _, f := range eq.ctx.Fields {
		if !ethnos.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if eq.path != nil {
		prev, err := eq.path(ctx)
		if err != nil {
			return err
		}
		eq.sql = prev
	}
	if ethnos.Policy == nil {
		return errors.New("ent: uninitialized ethnos.Policy (forgotten import ent/runtime?)")
	}
	if err := ethnos.Policy.EvalQuery(ctx, eq); err != nil {
		return err
	}
	return nil
}

func (eq *EthnosQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Ethnos, error) {
	var (
		nodes       = []*Ethnos{}
		_spec       = eq.querySpec()
		loadedTypes = [1]bool{
			eq.withArtifacts != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Ethnos).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Ethnos{config: eq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(eq.modifiers) > 0 {
		_spec.Modifiers = eq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, eq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := eq.withArtifacts; query != nil {
		if err := eq.loadArtifacts(ctx, query, nodes,
			func(n *Ethnos) { n.Edges.Artifacts = []*Artifact{} },
			func(n *Ethnos, e *Artifact) { n.Edges.Artifacts = append(n.Edges.Artifacts, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range eq.withNamedArtifacts {
		if err := eq.loadArtifacts(ctx, query, nodes,
			func(n *Ethnos) { n.appendNamedArtifacts(name) },
			func(n *Ethnos, e *Artifact) { n.appendNamedArtifacts(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range eq.loadTotal {
		if err := eq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (eq *EthnosQuery) loadArtifacts(ctx context.Context, query *ArtifactQuery, nodes []*Ethnos, init func(*Ethnos), assign func(*Ethnos, *Artifact)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Ethnos)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Artifact(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(ethnos.ArtifactsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ethnos_artifacts
		if fk == nil {
			return fmt.Errorf(`foreign-key "ethnos_artifacts" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "ethnos_artifacts" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (eq *EthnosQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := eq.querySpec()
	if len(eq.modifiers) > 0 {
		_spec.Modifiers = eq.modifiers
	}
	_spec.Node.Columns = eq.ctx.Fields
	if len(eq.ctx.Fields) > 0 {
		_spec.Unique = eq.ctx.Unique != nil && *eq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, eq.driver, _spec)
}

func (eq *EthnosQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(ethnos.Table, ethnos.Columns, sqlgraph.NewFieldSpec(ethnos.FieldID, field.TypeInt))
	_spec.From = eq.sql
	if unique := eq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if eq.path != nil {
		_spec.Unique = true
	}
	if fields := eq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, ethnos.FieldID)
		for i := range fields {
			if fields[i] != ethnos.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := eq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := eq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := eq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := eq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (eq *EthnosQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(eq.driver.Dialect())
	t1 := builder.Table(ethnos.Table)
	columns := eq.ctx.Fields
	if len(columns) == 0 {
		columns = ethnos.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if eq.sql != nil {
		selector = eq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if eq.ctx.Unique != nil && *eq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range eq.predicates {
		p(selector)
	}
	for _, p := range eq.order {
		p(selector)
	}
	if offset := eq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := eq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedArtifacts tells the query-builder to eager-load the nodes that are connected to the "artifacts"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (eq *EthnosQuery) WithNamedArtifacts(name string, opts ...func(*ArtifactQuery)) *EthnosQuery {
	query := (&ArtifactClient{config: eq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if eq.withNamedArtifacts == nil {
		eq.withNamedArtifacts = make(map[string]*ArtifactQuery)
	}
	eq.withNamedArtifacts[name] = query
	return eq
}

// EthnosGroupBy is the group-by builder for Ethnos entities.
type EthnosGroupBy struct {
	selector
	build *EthnosQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (egb *EthnosGroupBy) Aggregate(fns ...AggregateFunc) *EthnosGroupBy {
	egb.fns = append(egb.fns, fns...)
	return egb
}

// Scan applies the selector query and scans the result into the given value.
func (egb *EthnosGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, egb.build.ctx, "GroupBy")
	if err := egb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EthnosQuery, *EthnosGroupBy](ctx, egb.build, egb, egb.build.inters, v)
}

func (egb *EthnosGroupBy) sqlScan(ctx context.Context, root *EthnosQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(egb.fns))
	for _, fn := range egb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*egb.flds)+len(egb.fns))
		for _, f := range *egb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*egb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := egb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EthnosSelect is the builder for selecting fields of Ethnos entities.
type EthnosSelect struct {
	*EthnosQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (es *EthnosSelect) Aggregate(fns ...AggregateFunc) *EthnosSelect {
	es.fns = append(es.fns, fns...)
	return es
}

// Scan applies the selector query and scans the result into the given value.
func (es *EthnosSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, es.ctx, "Select")
	if err := es.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EthnosQuery, *EthnosSelect](ctx, es.EthnosQuery, es, es.inters, v)
}

func (es *EthnosSelect) sqlScan(ctx context.Context, root *EthnosQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(es.fns))
	for _, fn := range es.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*es.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := es.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
